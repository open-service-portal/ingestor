/**
 * Backstage Export Module
 *
 * Provides functionality to export entities from a running Backstage catalog.
 */

export { main, fetchEntities, buildFilter, exportEntities } from './cli/backstage-export-cli';
