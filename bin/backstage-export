#!/usr/bin/env node

/**
 * Backstage Export CLI entry point
 * This script uses ts-node to run the TypeScript source directly
 */

require('ts-node/register');
require('../src/backstage-export/cli/backstage-export-cli');