apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: {{slugify xrd.metadata.name}}
  title: {{xrd.spec.names.kind}}
spec:
  owner: test-owner
  type: test-resource
  parameters:
{{{indent parametersRendered 4}}}
  steps:
{{{indent stepsRendered 4}}}
  output:
{{{indent outputRendered 4}}}
